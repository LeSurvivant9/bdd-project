---
services:
  oracle-db:
    extends:
      file: oracle/docker-compose.yml
      service: oracle-db
  postgres-db:
    extends:
      file: postgres/docker-compose.yml
      service: postgres-db
volumes:
  oracle-data:
  postgres-data:
networks:
  postgres_net:
    driver: bridge
